import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Sky Gradient
  static const Color skyTop = Color(0xFF0B1028);
  static const Color skyMiddle = Color(0xFF4A3F91);
  static const Color skyBottom = Color(0xFFFF8A5B);

  // Sun
  static const Color sun = Color(0xFFFFD166);
  static const Color sunGlow = Color(0x66FFD166);

  // Clouds
  static const Color cloud = Color(0xFFFFFFFF);
  static const Color cloudShadow = Color(0x33FFFFFF);

  // Glass
  static const Color glass = Color(0x1AFFFFFF);
  static const Color glassBorder = Color(0x40FFFFFF);
  static const Color glassHighlight = Color(0x66FFFFFF);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFD9D9E3);

  // Weather
  static const Color warning = Color(0xFFFFC857);
  static const Color rain = Color(0xFF5BC0EB);
  static const Color wind = Color(0xFFA29BFE);

  // Shadows
  static const Color shadow = Color(0x33000000);

  // Particles
  static const Color particle = Color(0x80FFFFFF);
}